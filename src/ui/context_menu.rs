// Context menu functionality for file operations
use eframe::egui;

pub struct ContextMenu {
    // TODO: Implement context menu functionality
}

impl ContextMenu {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ContextMenu {
    fn default() -> Self {
        Self::new()
    }
}

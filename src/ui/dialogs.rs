// Dialog boxes for various operations
use eframe::egui;

pub struct Dialogs {
    // TODO: Implement dialogs for rename, delete confirmation, properties, etc.
}

impl Dialogs {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Dialogs {
    fn default() -> Self {
        Self::new()
    }
}
